def add_numbers(a, b):
    result = a + b
    print(result)


first_number_to_add = int(input('Enter a number? '))
second_number_to_add = int(input('Enter a second number to add '))

add_numbers(first_number_to_add, second_number_to_add)
